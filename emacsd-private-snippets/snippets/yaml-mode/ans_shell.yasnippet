# -*- mode: snippet -*-
# name: ans_shell
# key: shell
# --

# https://docs.ansible.com/ansible/latest/modules/shell_module.html#examples
- name: ${1:Type} RUN SHELL ${2:description of shell command}
  ${4:when: foo == "bar"}
  shell: |
    ${3:cat <<EOF > null.txt}
  args:
    executable: /bin/bash
    chdir: "{{ project_root }}"
    creates: null.txt
  tags:
    - $1_run
  register: some_output_var
$0
